name: Moonlight-Qt + ClawBridge (macOS)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-macos:
    runs-on: macos-15
    env:
      QT_VERSION: '6.10.2'
      APP_BIN: Moonlight-ClawBridge
      APP_DISPLAY: 'Moonlight ClawBridge'

    steps:
      - name: Checkout moonlight-qt source
        uses: actions/checkout@v4
        with:
          repository: moonlight-stream/moonlight-qt
          submodules: recursive
          fetch-depth: 1
          path: moonlight-qt

      - name: Checkout ClawBridge files
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          path: my_repo

      - name: Apply ClawBridge patch
        working-directory: moonlight-qt
        run: |
          git apply ../my_repo/_patches/claw_bridge.patch
          git diff --stat HEAD

      - name: Branding — rename app BEFORE build
        working-directory: moonlight-qt
        run: |
          set -e
          sed -i '' 's/TARGET = moonlight/TARGET = $APP_BIN/g' app/app.pro
          sed -i '' 's/TARGET = Moonlight/TARGET = $APP_BIN/g' app/app.pro
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName $APP_DISPLAY" app/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleExecutable $APP_BIN" app/Info.plist
          /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier com.clawbridge.ClawBridge" app/Info.plist
          echo "=== Verify ==="
          grep "TARGET" app/app.pro
          grep "CFBundle" app/Info.plist | grep -i "display\|executable\|identifier"

      - name: Install Qt 6
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}

      - name: qmake build
        working-directory: moonlight-qt
        run: |
          set -e
          BUILD_DIR="$PWD/build/Release"
          mkdir -p "$BUILD_DIR"
          cd "$BUILD_DIR"
          qmake ../../moonlight-qt.pro QMAKE_APPLE_DEVICE_ARCHS="x86_64 arm64" CONFIG+=release
          make -j$(sysctl -n hw.ncpu)
          echo "=== Built .app ==="
          ls -la app/

      - name: Create DMG
        working-directory: moonlight-qt
        run: |
          set -e
          VERSION=$(cat app/version.txt)
          BUILD_DIR="$PWD/build/Release"
          cd "$BUILD_DIR"
          macdeployqt app/$APP_BIN.app -qmldir=$PWD/../../app/gui -dmg
          DMG_OUT="../../Moonlight-ClawBridge-${VERSION}-macOS-Universal.dmg"
          mv app/$APP_BIN.dmg "$DMG_OUT"
          ls -lh "$DMG_OUT"

      - name: Upload DMG
        uses: actions/upload-artifact@v4
        with:
          name: Moonlight-ClawBridge-macOS-Universal
          path: moonlight-qt/Moonlight-ClawBridge-*.dmg
          compression-level: 0
          retention-days: 30
          if-no-files-found: error
